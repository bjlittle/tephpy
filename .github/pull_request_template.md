## Description

<!-- What does this PR change, and why? -->

## Checklist

- [ ] Added a `changelog/<PR>.<type>.rst` news fragment (or applied `skip-changelog`).
- [ ] Tests pass (`pixi run tests`).
- [ ] Pre-commit passes (`pixi run lint`).
- [ ] Docs build (`pixi run docs`) if docs changed.
- [ ] Prose reviewed against [*Reviewing Claims*][reviewing-claims] if docs changed —
      say which member of the set you counted, which tool and version you ran.

[reviewing-claims]: https://tephpy.readthedocs.io/en/latest/developer/docs-style.html#reviewing-claims
