Explanation
===========

Why the diagram is the shape it is. Nothing here is needed to use ``tephpy`` — the
:doc:`tutorials <../tutorials/index>` and :doc:`how-to guides <../howtos/index>`
stand on their own — but a :term:`tephigram` is an unusual chart, and its
oddities all follow from decisions worth understanding.

Two pages, each answering a question the tutorials raise and then move past. *Why
the Axes Are Rotated* covers the coordinate choice, the perpendicular grid and the
missing pressure axis. *Parcel Ascent and Normand's Point* covers what happens when
a :term:`parcel` of air is lifted, where cloud base comes from, and why the
energies a forecaster reads off the chart are areas rather than numbers.

Where a convention comes from a published chart rather than from the mathematics,
the page says so and cites it; :doc:`../reference/references` collects those
sources.

.. toctree::
    :maxdepth: 1

    rotated-axes
    parcel-ascent
